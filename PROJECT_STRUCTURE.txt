AI TOOLS - iOS Application
================================

PROJECT LOCATION: /Users/makarsidor/Desktop/iOS_Swift/AITools/

STRUCTURE:
├── AITools.xcodeproj/          (Generated by xcodegen)
├── project.yml                  (xcodegen configuration)
│
└── AITools/                     (Source code root)
    ├── AIToolsApp.swift         (App entry point)
    ├── Info.plist               (Bundle configuration)
    │
    ├── Core/
    │   ├── DesignSystem/
    │   │   ├── AppColors.swift          (Color palette + extensions)
    │   │   ├── AppFonts.swift           (Typography)
    │   │   └── Components/
    │   │       ├── GradientButton.swift (Reusable button with gradient)
    │   │       ├── AIToolCard.swift     (Tool cards - large & small)
    │   │       └── MessageBubble.swift  (Chat bubbles + typing indicator)
    │   │
    │   └── Extensions/
    │       └── View+Extensions.swift    (SwiftUI view helpers)
    │
    ├── Network/
    │   ├── APIClient.swift      (Async/await HTTP client - actor)
    │   └── DTOs/
    │       ├── ChatDTO.swift    (Chat request/response models)
    │       ├── WritingDTO.swift (Writing enums & models)
    │       └── VideoDTO.swift   (Video generation models)
    │
    ├── Services/
    │   └── ApphudService.swift  (Subscription service - mock)
    │
    └── Features/
        ├── Home/
        │   ├── HomeView.swift        (Main screen with tool grid)
        │   └── HomeViewModel.swift   (Tool info management)
        │
        ├── AIChat/
        │   ├── AIChatView.swift      (Chat interface with auto-scroll)
        │   └── AIChatViewModel.swift (Message management)
        │
        ├── AIWriting/
        │   ├── AIWritingView.swift      (Writing improvement tool)
        │   └── AIWritingViewModel.swift (Text processing logic)
        │
        ├── VideoGenerator/
        │   ├── VideoGeneratorView.swift      (Video creation interface)
        │   └── VideoGeneratorViewModel.swift (Video state management)
        │
        ├── Paywall/
        │   ├── PaywallView.swift       (Subscription UI)
        │   └── PaywallViewModel.swift  (Product management)
        │
        └── Settings/
            └── SettingsView.swift      (App settings & links)

ARCHITECTURE:
- MVVM + Clean Architecture
- @MainActor on all ViewModels
- @StateObject + @ObservedObject for iOS 16 compatibility
- actor-based APIClient for strict concurrency
- SwiftUI NavigationStack for routing

FEATURES IMPLEMENTED:
✓ Home screen with gradient-styled tool cards
✓ AI Chat with auto-scrolling messages & typing indicator
✓ AI Writing with character counter & action selection
✓ Video Generator with progress states
✓ Paywall with subscription options
✓ Settings with links & subscription management
✓ Design system (colors, fonts, components)
✓ Network layer with async/await
✓ Mock Apphud service

DESIGN SYSTEM:
- Background: #0A0A0F (dark with blue tint)
- Cards: #1A1A28, #252535
- Primary gradient: #E91E8C → #7B2FBE (pink to purple)
- Text: white & #8E8E9A (secondary)
- Input: #1C1C2A
- Separator: #2A2A3A

COMPILATION STATUS:
✓ No errors
✓ Builds successfully for iOS 16.0+
✓ Runs on iPhone 15 simulator

API ENDPOINTS (Placeholders):
- POST /v1/chat
- POST /v1/writing
- POST /v1/video

DEPLOYMENT TARGET: iOS 16.0+
SWIFT VERSION: 5.9+
